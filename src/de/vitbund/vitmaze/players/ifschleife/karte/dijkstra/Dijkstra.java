package de.vitbund.vitmaze.players.ifschleife.karte.dijkstra;

import java.util.HashMap;
import java.util.Map;

import de.vitbund.vitmaze.players.ifschleife.karte.Feld;
import de.vitbund.vitmaze.players.ifschleife.karte.Karte;

public class Dijkstra {
	
	//private Map gefundeneBegehbareFelder;
	
	private HashMap erstelleListen(Karte karte, int xStart, int yStart) {
	/*	if(karte == null) { //falsche Übergabe abfangen
			return null; 
		}
		HashMap<Feld,]> gefundeneBegehbareFelder = new HashMap();
		
		for (Feld[] felder: karte.getFelder()) {
			for (Feld feld : felder) {
				if(feld != null) {
					
				}
			}
		}
		
		*/
		return null;
	}

}
